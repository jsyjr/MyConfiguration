# Dash source - copy all run artifacts from network qualification area to local workspace

echo Saving to "${SAVERUN}".

( mkdir -p ${TESTDIR} \
; rm -rf  ${SAVERUN} \
; cp -rf $s/qualify_sbruntests ${SAVERUN} \
; echo  $s/qualify_sbruntests copied to ${SAVERUN} \
; echo \
)
